import styled from 'styled-components';

export const StyledNewProductIndicator = styled.p`
  color: ${({ theme }) => theme.colors.primary};
  text-transform: uppercase;
  font-size: 1.4rem;
  line-height: 1.9rem;
  letter-spacing: 10px;
`;
